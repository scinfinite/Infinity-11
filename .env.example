# Non-secret development configuration
NODE_ENV=development
LOG_LEVEL=info
INFINITY_PUBLIC_ORIGIN=http://localhost:3000

# Provider credentials are intentionally not represented here.
# Store real secrets in the environment/secret manager used by the deployment boundary.
